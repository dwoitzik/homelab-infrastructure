{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "docker:pinDigests", "helpers:pinGitHubActionDigests"],
  "kubernetes": {
    "fileMatch": ["kubernetes/.+\\.yml$"]
  },
  "minimumReleaseAge": "3 days",
  "packageRules": [
    {
      "description": "Stateless services + digest pins: patch/minor/digest/pin only, auto-merge after CI green + a 3-day soak so a bad upstream release doesn't land same-day. 'pin' covers first-time digest pinning (no prior digest to diff against), which otherwise sits stuck needing a manual merge forever -- same soak/CI gate applies. Rule order matters -- the stateful/critical rule below is listed last so it overrides this one for the specific packages it names.",
      "matchManagers": ["kubernetes"],
      "matchUpdateTypes": ["patch", "minor", "digest", "pin"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "CI tooling (GitHub Actions): low risk, easy to revert, auto-merge after soak. 'pin' included for the same first-time-pinning reason as above.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["patch", "minor", "digest", "pin"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "Dev tooling (pre-commit hooks, Terraform providers): doesn't touch live services, auto-merge after soak. 'pin' included for the same first-time-pinning reason as above.",
      "matchManagers": ["pre-commit", "terraform"],
      "matchUpdateTypes": ["patch", "minor", "digest", "pin"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "Major updates, any package: always PR-only, one PR per package (not grouped -- each major is its own migration, shouldn't be bundled with an unrelated one).",
      "matchManagers": ["kubernetes"],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["major-update"]
    },
    {
      "description": "Terraform provider majors: made explicit rather than relying on Renovate's default (automerge unset = false). This is the exact bump class that caused the 2026-07-09 OOM/outage (hashicorp/aws 5.x->6.x) -- a major provider bump can pull a full new release's worth of platform builds through the terraform manager's checksum-verification path, so it always needs a human glance before merge, not just a green CI run.",
      "matchManagers": ["terraform"],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["major-update"]
    },
    {
      "description": "Stateful/critical services -- databases, Vault, Authelia, Garage, Vaultwarden, and apps whose upgrades need a manual data migration (Nextcloud, Paperless, Immich, Gitea) or whose breakage takes out backups/auth cluster-wide (Velero's own restic/kopia plugin). ALL update types (patch/minor/major) stay PR-only, grouped into one PR per bump wave so they're one glance instead of scattered across the inbox. Listed last so it wins over the stateless auto-merge rule above for these specific packages. icereed/paperless-gpt was added after a version auto-merged and immediately CrashLoopBackOff'd -- its entrypoint isn't stable against this repo's readOnlyRootFilesystem hardening across versions, needs manual verification per bump.",
      "matchManagers": ["kubernetes"],
      "matchPackageNames": [
        "hashicorp/vault",
        "ghcr.io/authelia/authelia",
        "dxflrs/garage",
        "vaultwarden/server",
        "postgres",
        "ghcr.io/immich-app/postgres",
        "redis",
        "valkey/valkey",
        "nextcloud",
        "paperlessngx/paperless-ngx",
        "gitea/gitea",
        "ghcr.io/immich-app/immich-server",
        "ghcr.io/immich-app/immich-machine-learning",
        "velero/velero-plugin-for-aws",
        "icereed/paperless-gpt"
      ],
      "groupName": "stateful/critical services",
      "labels": ["stateful-critical"],
      "automerge": false
    }
  ],
  "hostRules": [
    {
      "description": "2026-07-09 OOM root cause: a major hashicorp/aws provider bump (5.x->6.53.0) makes Renovate's terraform manager fetch and SHA256-hash every platform build of the release (14 builds) to keep .terraform.lock.hcl valid. Renovate's default concurrentRequestLimit (16) meant up to 16 of those provider zips could be buffered in memory at once, producing a real transient peak of ~4GiB in a single ~30s window (confirmed via an isolated, generously-capped diagnostic run -- not a leak, the process finished cleanly at exit 0 given real headroom). Under the CronJob's tight memory limit, the cgroup killed it mid-spike before GC could reclaim. Capping concurrency here flattens the peak at the source instead of just hoping a bigger ceiling survives it.",
      "matchHost": "releases.hashicorp.com",
      "concurrentRequestLimit": 2
    }
  ]
}
