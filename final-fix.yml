# Authelia
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: auth-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`auth.woitzik.dev`)
    kind: Rule
    services: [{name: authelia, port: 9091}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Home
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: home-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`home.woitzik.dev`)
    kind: Rule
    middlewares: [{name: authelia}]
    services: [{name: homepage, port: 80}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Docs
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: docs-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`docs.woitzik.dev`)
    kind: Rule
    middlewares: [{name: authelia}]
    services: [{name: paperless, port: 8000}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Argo
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argo-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`argo.woitzik.dev`)
    kind: Rule
    services: [{name: argocd-server, namespace: argocd, port: 80}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Monitoring
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: monitor-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`monitoring.woitzik.dev`)
    kind: Rule
    services: [{name: kube-prometheus-stack-grafana, namespace: monitoring, port: 80}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# PVE
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pve-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`pve.woitzik.dev`)
    kind: Rule
    services: [{name: external-pve, port: 8006, scheme: https, serversTransport: insecure-transport}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Traefik
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`traefik.woitzik.dev`)
    kind: Rule
    middlewares: [{name: authelia}]
    services: [{name: api@internal, kind: TraefikService}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Longhorn
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`longhorn.woitzik.dev`)
    kind: Rule
    middlewares: [{name: authelia}]
    services: [{name: longhorn-frontend, namespace: longhorn-system, port: 80}]
  tls: {secretName: wildcard-woitzik-dev-tls}
---
# Minio
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`minio.woitzik.dev`)
    kind: Rule
    middlewares: [{name: authelia}]
    services: [{name: minio, port: 9001}]
  tls: {secretName: wildcard-woitzik-dev-tls}
