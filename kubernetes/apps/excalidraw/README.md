# Excalidraw

Self-hosted whiteboard/diagramming tool (`draw.woitzik.dev`).

## Storage

None — stateless. Excalidraw's own boards are stored client-side (browser local
storage) unless explicitly exported/shared; the server here just serves the app itself.

## How to restore

Nothing to restore — re-apply the manifests, pod comes back with zero state to recover.
