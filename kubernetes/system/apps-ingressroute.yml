# Authelia
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: auth-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`auth.woitzik.dev`)
    kind: Rule
    services: [{name: authelia, port: 9091}]
---
# Homepage
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: home-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`home.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: homepage, port: 80}]
---
# Paperless
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: docs-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`docs.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: paperless, port: 8000}]
---
# AI
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ai-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`ai.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: open-webui, port: 8080}]
---
# Immich
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`photos.woitzik.dev`)
    kind: Rule
    # SEC hardening (2026-07-08): no rate limiting existed on this route at
    # all -- Immich has no external auth in front of it (mobile app needs
    # API-token auth, not Authelia's browser redirect), so this is the only
    # brute-force/abuse mitigation on login and upload. See
    # apps/immich/middleware.yml for the limit rationale.
    middlewares: [{name: immich-ratelimit}]
    services: [{name: immich-server, port: 2283}]
---
# Vaultwarden — direct access, no Authelia in front (2026-08-08).
# Vaultwarden's own 2FA is the only gate.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vaultwarden-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`vault.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}]
    services: [{name: vaultwarden, port: 80}]
---
# AdGuard Home external service (primary RPi at 10.0.20.2:3001)
apiVersion: v1
kind: Service
metadata:
  name: external-dns
  namespace: apps
spec:
  clusterIP: None
  ports:
  - port: 3001
    targetPort: 3001
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-dns
  namespace: apps
subsets:
- addresses:
  - ip: 10.0.20.2
  ports:
  - port: 3001
---
# DNS (AdGuard)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dns-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`dns.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: external-dns, port: 3001}]
---
# Garage API (S3)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: garage-s3-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`s3.woitzik.dev`)
    kind: Rule
    services: [{name: garage, port: 3900}]
---
# Proxmox (PVE)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pve-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`pve.woitzik.dev`)
    kind: Rule
    middlewares:
      - name: crowdsec-bouncer
      - name: authelia
      - name: websockets
    services:
      - name: external-pve
        port: 8006
        scheme: https
        serversTransport: insecure-transport
---
# Proxmox Backup (PBS)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pbs-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`backup.woitzik.dev`)
    kind: Rule
    middlewares:
      - name: crowdsec-bouncer
      - name: authelia
      - name: websockets
    services:
      - name: external-pbs
        port: 8007
        scheme: https
        serversTransport: insecure-transport
        passHostHeader: true
---
# External services for pve-final / pbs-final — these are manual Services +
# Endpoints pointing at host-ported apps (PVE mgmt on 10.0.10.10, PBS on
# 10.0.10.110). Were NOT git-tracked until 2026-08-08 (the PVE/PBS outage was
# partly caused by them living only as untracked live objects).
apiVersion: v1
kind: Service
metadata:
  name: external-pve
  namespace: apps
spec:
  ports:
  - port: 8006
    targetPort: 8006
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-pve
  namespace: apps
subsets:
- addresses:
  - ip: 10.0.10.10
  ports:
  - port: 8006
---
apiVersion: v1
kind: Service
metadata:
  name: external-pbs
  namespace: apps
spec:
  ports:
  - port: 8007
    targetPort: 8007
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-pbs
  namespace: apps
subsets:
- addresses:
  - ip: 10.0.10.110
  ports:
  - port: 8007
---
# SEC-008 regression fix (2026-07-05): ADR-012 moved Atlantis off k3s onto its
# own LXC (ct-srv-atlantis-01, 10.0.20.250) and, in the process, deleted this
# IngressRoute entirely (it used to carry the Authelia gate) and repointed the
# Cloudflare Tunnel straight at the LXC's IP, bypassing Traefik/Authelia
# completely. Atlantis has no built-in auth of its own (no basic-auth env var
# configured) -- confirmed live: atlantis.woitzik.dev served its full UI
# (PR history, plan/apply state, lock controls) to an unauthenticated public
# request. Re-adding the same external-host + Authelia pattern already used
# for pve-final/pbs-final above. The Cloudflare Tunnel ingress (terraform/
# stacks/cloudflare/main.tf) must also be repointed to Traefik, not the LXC
# directly, for this to actually take effect -- see that file's matching fix.
apiVersion: v1
kind: Service
metadata:
  name: external-atlantis
  namespace: apps
spec:
  ports:
    - port: 4141
      targetPort: 4141
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-atlantis
  namespace: apps
subsets:
  - addresses:
      - ip: 10.0.20.250
    ports:
      - port: 4141
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: atlantis-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  # GitHub webhook path -- no Authelia (its own HMAC secret is the auth;
  # forcing it through Authelia's forward-auth redirect would break delivery
  # entirely, per the original SEC-008 fix this replicates).
  - match: Host(`atlantis.woitzik.dev`) && PathPrefix(`/events`)
    kind: Rule
    priority: 10
    services: [{name: external-atlantis, port: 4141}]
  - match: Host(`atlantis.woitzik.dev`)
    kind: Rule
    priority: 1
    middlewares:
      - name: crowdsec-bouncer
      - name: authelia
      - name: websockets
    services: [{name: external-atlantis, port: 4141}]
---
# Uptime Kuma
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: status-final
  namespace: monitoring
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`status.woitzik.dev`)
    kind: Rule
    middlewares:
      - name: crowdsec-bouncer
        namespace: apps
      - name: authelia
        namespace: apps
    services: [{name: uptime-kuma, port: 3001}]
---
# Headscale (Management VPN)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: headscale-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`headscale.woitzik.dev`)
    kind: Rule
    middlewares: [{name: websockets}]
    services: [{name: headscale, port: 8080}]
---
# Mealie (Recipe Manager)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mealie-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`mealie.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: mealie, port: 9000}]
---
# Nextcloud (no Authelia — CalDAV/CardDAV native clients need direct access)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`nextcloud.woitzik.dev`)
    kind: Rule
    services: [{name: nextcloud, port: 80}]
---
# Gitea (private git) — Authelia added (2026-07-17); web UI now gated.
# Git SSH and token-based API access are unaffected.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitea-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`git.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: gitea, port: 3000}]
---
# Home Assistant (Authelia guards web UI; Companion app should use Headscale VPN
# for direct local access, same pattern as Jellyfin below)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ha-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`ha.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: home-assistant, port: 8123}]
---
# Jellyfin — no Authelia; Jellyfin's own auth handles access control.
# Cloudflare tunnel also bypasses Authelia (direct to Jellyfin login), so this
# makes the internal path consistent. App clients (Android/iOS) cannot handle
# Authelia's SSO redirect flow.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: media-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`media.woitzik.dev`)
    kind: Rule
    services: [{name: jellyfin, port: 8096}]
# SABnzbd, Sonarr, Radarr, Bazarr — IngressRoutes consolidated into
# kubernetes/apps/jellyfin/usenet.yml alongside their External service definitions.
---
# Scrutiny — S.M.A.R.T. disk health monitoring
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: scrutiny-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`scrutiny.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}]
    services: [{name: scrutiny-web, port: 8080}]
---
# Wazuh (SIEM on ct-srv-docker-01, 10.0.20.252:443) — own auth, no Authelia
apiVersion: v1
kind: Service
metadata:
  name: external-wazuh
  namespace: apps
spec:
  ports:
    - port: 443
      targetPort: 443
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-wazuh
  namespace: apps
subsets:
  - addresses:
      - ip: 10.0.20.252
    ports:
      - port: 443
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wazuh-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`wazuh.woitzik.dev`)
    kind: Rule
    middlewares:
      - name: crowdsec-bouncer
    services:
      - name: external-wazuh
        port: 443
        scheme: https
        serversTransport: insecure-transport
---
# Claude Code agent web terminal (ttyd on ct-srv-claude-agent, LXC 100,
# 10.0.100.253:7681). Previously reachable only via an AdGuard split-horizon
# rewrite (LAN/Tailscale only) -- added here so claude.woitzik.dev works from
# outside the LAN too (Cloudflare Tunnel wildcard rule -> here), per the
# operator's explicit ask. ttyd's own password auth stays as a second factor;
# Authelia is the primary gate, same as every other admin-facing service.
apiVersion: v1
kind: Service
metadata:
  name: external-claude-agent
  namespace: apps
spec:
  ports:
    - port: 7681
      targetPort: 7681
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-claude-agent
  namespace: apps
subsets:
  - addresses:
      - ip: 10.0.100.253
    ports:
      - port: 7681
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: claude-final
  namespace: apps
spec:
  entryPoints: [websecure]
  routes:
  - match: Host(`claude.woitzik.dev`)
    kind: Rule
    middlewares: [{name: crowdsec-bouncer}, {name: authelia}, {name: websockets}]
    services: [{name: external-claude-agent, port: 7681}]
